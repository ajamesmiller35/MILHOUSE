
console.log('working!');

/*$(window).resize(function(){
  var width = $(window).width();
  var ratio = width/1680;
  var waveWidth = width * ratio;

  $('#waveform').css('width', waveWidth);
  console.log(waveWidth);
});
*/
